export const MARKET_WEIGHTS = Object.freeze({
  demand: 0.5,
  scarcity: 0.3,
  trend: 0.2,
});


export const MARKET_RULES = Object.freeze({
  minimumHistoricalObservations: 4,
  scarcityMultiplier: 125,
  trendMultiplier: 500,
});